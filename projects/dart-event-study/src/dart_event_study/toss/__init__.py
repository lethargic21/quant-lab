"""토스증권 커뮤니티 순방향(forward) 수집.

핵심 설계: 토스는 절대 시각을 주지 않고 상대 시각("3시간 전"→"3일 전")만 준다.
따라서 **글이 처음 관측된 크롤 시각(first_seen_at)** 을 우리가 찍어 사실상의 타임스탬프로 쓴다.
과거 백필 불가 — 오늘부터 순방향으로만 쌓는다.

제약: 실제 Chromium(UA 무변조), 렌더된 DOM만 읽음, WS 프레임/프라이빗 API 미접근,
robots 준수, 로그인 없음, 작성자 닉네임은 salted hash로만(원문 미저장).
"""
